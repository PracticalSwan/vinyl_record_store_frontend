# Frontend Library

`api.js` is the canonical backend client. Keep request URLs, envelope handling, connection errors, and public API configuration in this boundary.

Database helpers and recommendation scoring belong in the backend repository.
